services:
  - type: web
    name: history-zoom
    runtime: php
    buildCommand: echo "ready"
    startCommand: php -S 0.0.0.0:$PORT router.php
    envVars:
      - key: PORT
        value: 10000
    disk:
      name: csvdata
      mountPath: /var/data
      sizeGB: 1
